import React from 'react';

import { Template } from '../../sharedComponents/Template';
import { CrossReferenceBody } from './CrossReferenceBody';

export const CrossReference = () => (
  <Template>
    <CrossReferenceBody />
  </Template>
);
